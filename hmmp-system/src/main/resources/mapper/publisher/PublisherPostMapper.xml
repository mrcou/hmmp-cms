<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hmmp.system.mapper.publisher.PublisherPostMapper">

    <resultMap type="com.hmmp.system.domain.publisher.PublisherPost" id="PublisherPostResult">
        <id     property="postId"           column="post_id"            />
        <result property="orderId"          column="order_id"           />
        <result property="postType"         column="post_type"          />
        <result property="shippingName"     column="shipping_name"      />
        <result property="shippingPhone"    column="shipping_phone"     />
        <result property="shippingAddress"  column="shipping_address"   />
        <result property="shippingZipCode"  column="shipping_zip_code"  />
        <result property="trackingNo"       column="tracking_no"        />
        <result property="courier"          column="courier"            />
        <result property="postStatus"       column="post_status"        />
        <result property="postTime"         column="post_time"          />
        <result property="signTime"         column="sign_time"          />
        <result property="createBy"         column="create_by"          />
        <result property="createTime"       column="create_time"        />
        <result property="updateBy"         column="update_by"          />
        <result property="updateTime"       column="update_time"        />
        <result property="remark"           column="remark"             />
    </resultMap>

    <sql id="selectPublisherPostVo">
        select post_id, order_id, post_type, shipping_name, shipping_phone, shipping_address, shipping_zip_code,
               tracking_no, courier, post_status, post_time, sign_time, create_by, create_time, update_by, update_time, remark
        from publisher_post
    </sql>

    <select id="selectPostList" parameterType="com.hmmp.system.domain.publisher.PublisherPost" resultMap="PublisherPostResult">
        <include refid="selectPublisherPostVo"/>
        <where>
            <if test="orderId != null">AND order_id = #{orderId}</if>
            <if test="postType != null and postType != ''">AND post_type = #{postType}</if>
            <if test="trackingNo != null and trackingNo != ''">AND tracking_no like concat('%', #{trackingNo}, '%')</if>
            <if test="postStatus != null and postStatus != ''">AND post_status = #{postStatus}</if>
        </where>
        order by create_time desc
    </select>

    <select id="selectPostById" parameterType="Long" resultMap="PublisherPostResult">
        <include refid="selectPublisherPostVo"/>
        where post_id = #{postId}
    </select>

    <insert id="insertPost" parameterType="com.hmmp.system.domain.publisher.PublisherPost" useGeneratedKeys="true" keyProperty="postId">
        insert into publisher_post (
            order_id, post_type, shipping_name, shipping_phone, shipping_address, shipping_zip_code,
            tracking_no, courier, post_status, post_time, sign_time, create_by, create_time, update_by, update_time, remark
        ) values (
            #{orderId}, #{postType}, #{shippingName}, #{shippingPhone}, #{shippingAddress}, #{shippingZipCode},
            #{trackingNo}, #{courier}, #{postStatus}, #{postTime}, #{signTime}, #{createBy}, sysdate(), #{updateBy}, sysdate(), #{remark}
        )
    </insert>

    <update id="updatePost" parameterType="com.hmmp.system.domain.publisher.PublisherPost">
        update publisher_post
        <set>
            <if test="orderId != null">order_id = #{orderId},</if>
            <if test="postType != null">post_type = #{postType},</if>
            <if test="shippingName != null">shipping_name = #{shippingName},</if>
            <if test="shippingPhone != null">shipping_phone = #{shippingPhone},</if>
            <if test="shippingAddress != null">shipping_address = #{shippingAddress},</if>
            <if test="shippingZipCode != null">shipping_zip_code = #{shippingZipCode},</if>
            <if test="trackingNo != null">tracking_no = #{trackingNo},</if>
            <if test="courier != null">courier = #{courier},</if>
            <if test="postStatus != null">post_status = #{postStatus},</if>
            <if test="postTime != null">post_time = #{postTime},</if>
            <if test="signTime != null">sign_time = #{signTime},</if>
            update_by = #{updateBy},
            update_time = sysdate(),
            <if test="remark != null">remark = #{remark},</if>
        </set>
        where post_id = #{postId}
    </update>

    <update id="updatePostStatus" parameterType="com.hmmp.system.domain.publisher.PublisherPost">
        update publisher_post set post_status = #{postStatus}, tracking_no = #{trackingNo}, courier = #{courier},
        post_time = #{postTime}, update_by = #{updateBy}, update_time = sysdate()
        where post_id = #{postId}
    </update>

    <delete id="deletePostById" parameterType="Long">
        delete from publisher_post where post_id = #{postId}
    </delete>

    <delete id="deletePostByIds" parameterType="Long">
        delete from publisher_post where post_id in
        <foreach collection="array" item="postId" open="(" separator="," close=")">
            #{postId}
        </foreach>
    </delete>

</mapper>